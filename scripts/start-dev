#!/bin/bash
set -e

if [ "$MONGO_DIR" = "" ];then
    . `dirname "$0"`/environ.sh
fi

# export NODE_OPTIONS='--prof'
# send SIGUSR1 to debug

fuser -s "$MONGO_DIR/mongod.lock" || ./scripts/startdb.sh "$1"
cd app
exec node ../lib/koru.js "$@"
